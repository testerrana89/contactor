import { ContactList } from "@contact-hub/components";
import { Container } from "@mui/material";

export default function ContactPage() {
  return (
    <Container maxWidth="lg">
      <ContactList />
    </Container>
  );
}
